---
"@jade-garden/cli": minor
---

- **MAJOR**:
  - Removes CSS generation
  - Simplifies CLI configuration
  - Renames `components` to `styles` in configuration
- Fixes
  - Variable and export names (uses kebab case)
  - Move export of CLI configuration type out of `index.ts` file
- Added tests
