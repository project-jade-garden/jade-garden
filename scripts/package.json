{
	"name": "@project-jade-garden/scripts",
	"type": "module",
	"private": true,
	"scripts": {
		"create": "bun run src/create.ts",
		"setup": "bun run src/setup.ts",
		"clean": "rm -rf .turbo dist node_modules",
		"format": "biome format --write .",
		"lint": "biome lint",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@biomejs/biome": "catalog:",
		"cheerio": "1.1.2",
		"es-toolkit": "catalog:",
		"globby": "catalog:build",
		"typescript": "catalog:"
	}
}
